# UI Refresh — Feedback Wave 2 + Factions/Augmentations + Global Theming

> superpowers:subagent-driven-development on branch UIRefresh (base f1a9c160f). All prior Global Constraints + the CLUNKY-IS-A-FEATURE principle bind. Gates per task (tsc, full jest, lint, `npx webpack --mode development`), one commit per task w/ Fable trailer.

### Task W1 — World map: vectorized ASCII continents + no legend
User: continents "just vaguely right… you can do better." Replace the hand-traced outline polygons with a **vectorized rendering of the original ASCII art itself**: parse `src/ui/React/WorldMap.tsx` art lines (the anti-drift parser from F3's test shows how); every non-space, non-city-letter character at (col,row) becomes a short SVG stroke through the SAME artToSvg projection the cities use, oriented by glyph: `/`→45° rising, `\`→falling, `|`→vertical, `-`/`_`→horizontal, `~`/`,`/`'`/`.`→short horizontal tick (subtle). Stroke `borderDefault`-family, ~1-1.5px, round caps; slight opacity variance ok. Result: the continents ARE the original map's shapes by construction. Delete the legend entirely (current-city/invite/nothing rows). Keep graticule, nodes, arcs, popover, index column. Move the art parsing into `worldMapData.ts` (runtime parse of an embedded copy OR import the art from a shared module extracted out of WorldMap.tsx so ASCII page + vector map share one source — prefer extraction to `src/ui/React/worldMapArt.ts` consumed by both; anti-drift test then compares directly). Tests: stroke count > threshold, city letters NOT rendered as strokes, shared-source assertion.

### Task W2 — City maps: multiple colored subway lines (aesthetic, not categorical)
User liked colored lines, not category organization: "make it look more like a real city subway map." Keep F4's art-derived station positions + connectivity. Decompose each city's network into 2-4 named-by-color ROUTES like a real transit map: longest path through the MST = Line 1; remove its edges; each remaining branch chain = next line (attach at the shared station like a real interchange). Assign colors purely aesthetically from a fixed palette (accentGold, accentGreen, accentViolet, accentPink — rotate; NO meaning, NO legend, NO category logic returning). Where two lines share a station draw both stops (small offset or ring). 45°/90° routing kept; parallel offset (~4px) where lines overlap a segment. Station stroke = its line color (interchange = white ring like classic maps). Tests: every station on ≥1 route; union of routes = MST edge set (or superset if interchange duplication); per-city route count 2-4 for cities with >4 stations; deterministic output.

### Task W3 — Retire the floating overview; HUD everywhere
User: "The floating overview should be replaced altogether on all screens and replaced by the pinned sidebar."
- `Page.Work` (focus screen) renders INSIDE ShellLayout (rail+topbar+HUD) — remove it from the `withSidebar=false` set in GameRoot. Leaving Work via rail already triggers the existing stopFocusing logic (GameRoot lines ~271-273) — verify preserved.
- Remaining full-bleed pages (Recovery, BitVerse, Infiltration, BladeburnerCinematic, ImportSave/loading): NO overview of any kind (BitVerse/Recovery already self-hid; Infiltration/Cinematic lose the floating one — distraction-free is correct there).
- Floating `<Overview>`/`CharacterOverview` mount in GameRoot: removed EXCEPT the interactive tutorial path — `InteractiveTutorialRoot` currently renders inside the Overview wrapper; keep `<Overview>` mounted ONLY while `ITutorial.isRunning` (verify how GameRoot chooses tutorial vs overview today and preserve tutorial behavior exactly).
- `Settings.HudCollapsed` semantics: collapsed = HUD hidden, TopBar reopen button (already exists). No floating fallback. Update the Task 4-era mutual-exclusion tests (Hud.test / GameRoot expectations) to the new matrix: HUD on all shell pages incl. Work; floating only during tutorial.
- CharacterOverview component stays in-tree (tutorial wrapper + potential future use); dead props cleaned.

### Task W4 — Editor: explorer always visible (small)
`src/ScriptEditor/ui/ScriptEditorRoot.tsx` renders its shell (activity bar/explorer/status bar) only when a script is open (find the gate — likely the empty-openScripts early return). Fix: the editor page ALWAYS renders activity bar + explorer + status bar (+ bottom panel access); with no open file the editor pane shows a quiet placeholder ("open a file from the explorer / Ctrl+P") and the explorer lists the current server's files (Player.getCurrentServer()) since there's no currentScript hostname — define explorer server = currentScript?.hostname ?? Player's current server. Status bar segments that need a file (RAM/Ln/Col/lang) show placeholders. Tests: render with zero open scripts → explorer present, placeholder present, no crash; opening from explorer in that state works.

### Task W5 — Global theming for all remaining pages
Leverage point: the global MUI component styleOverrides in `src/Themes/ui/Theme.tsx` (MuiButton/Paper/Input*/Select/Menu/Tooltip/Tabs/Tab/Accordion*/Divider/Switch/Alert/TablePagination/etc.) currently map to LEGACY tokens (well, backgroundsecondary, primarydark…). Remap them to the new token system so every not-yet-reworked page (Stats, Hacknet, Sleeves, Bladeburner, Corporation, Gang, Stock Market, Milestones, Docs, Achievements, Options, locations…) inherits the new look: papers/cards `bgPanel` + `borderCard` + radius 8-10; buttons per refresh grammar (radius 6, bgPanel bg, borderCard border, hover borderFocus + bgActive; contained-primary = accentCyan w/ dark text); inputs `bgPanelDeep` + borderCard, focus borderFocus; tooltips bgPanelDeep/borderCard; tabs per ActiveScriptsRoot's restyle; body bg `bgApp`; default Typography color → `textBody` (KEEP semantic colors — money/hp/hack/combat/cha/int/rep and success/error/warning palettes — untouched; scripts and legacy screens rely on them). Palette mapping: primary.main stays Settings.theme.primary for MUI-semantic uses BUT audit what breaks — implementer judgment with screenshots-by-reasoning; the goal is chrome adopts the new shell language while user-meaningful colors persist. Verify at least: Stats, Hacknet, Gang, Options render sanely (jest render where cheap + manual checklist entry). This intentionally changes legacy screens' green-on-black text to slate `textBody` — per user's ask to theme all pages.

### Task W6 — Factions screen (1A)
Per `.superpowers/sdd/design-notes-1A-shell.md` factions section (gang hero card, faction rows, sort chips) reworking `src/Faction/ui/FactionsRoot.tsx`. Faction rows: name + category tag chip (HACK/CORP/ENDGAME per FactionInfo grouping — verify a sensible mapping exists; if none, drop chips rather than invent lore), current work subline, rep progress bar toward next affordable/unowned augmentation (`<delta> to <aug name>` — computed from existing rep + aug rep requirements the game already exposes on these screens), favor (mono), "N augments" pill (green when purchasable remain), chevron → faction details (existing Router.toPage(Page.Faction, {faction})). Your Gang hero card only when Player.gang. Sort chips: Closest to unlock / Reputation / Favor; toggle "Can buy augments" (sort/filter existing list only; default = game's current order; persist choice as `Settings.FactionsSort`/`FactionsCanBuyOnly` UI prefs). Invitations section preserved (accept/reject flows untouched). Honesty: only joined + invited + rumored factions as today's screen shows them (rumors section keeps its current presentation — don't enrich).

### Task W7 — Faction Augmentations screen (1B)
Design notes: `.superpowers/sdd/design-notes-1B-augmentations.md` (extracted in this wave) reworking `src/Faction/ui/AugmentationsPage.tsx`. Context strip (faction name + rep/favor/price-mult cluster); tabs Purchasable/Locked/Owned with counts (Owned collapses to chip cloud "+ N more"); purchasable cards 2-col: name, READY / CAN'T AFFORD tag, one-line effect summary (existing aug descriptions — truncate, full on tooltip/expand), ✓/✗ price + ✓/✗ rep rows colored by met-ness, Queue button (existing purchase flow + confirmation modal untouched — "Queue" = the existing buy action; verify what the current button does and keep semantics identical). Locked: dimmed cards + "Unlocks at N rep" progress. Existing sort/filter options on the page preserved. NO new derived analytics beyond what the mock + current page show.

### Task W8 — Wave gates + final review + push
Full gates; whole-wave review (most capable model) over f1a9c160f..HEAD incl. W5's blast radius; fix wave; push. Manual checklist: world map continents read, subway multi-line aesthetics, Work-page HUD, tutorial flow, legacy screens post-W5 (Stats/Hacknet/Gang/Corp/Options), factions/augs flows end-to-end.

**Order:** W1 → W2 → W3 → W4 → W5 → W6 → W7 → W8. (1B design extraction runs first, in parallel with W1.)
