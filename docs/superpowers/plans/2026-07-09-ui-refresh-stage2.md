# Bitburner UI Refresh — Stage 2: Terminal (2A), Active Scripts (1F), Script Editor (2C)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Stage 1 (foundation/shell/maps) is complete at commit 8154cf92d and pushed. Same Global Constraints as `2026-07-09-ui-refresh-foundation.md` (UI-only, tokens-only via `theme.colors.* as string`, mono via `Settings.styles.monoFontFamily`, existing formatters, no 100vh inside shell pane, tsc/test/lint/webpack gates per task, one commit per task ending with the Fable co-author line).

**Design fidelity sources:** `.superpowers/sdd/design-notes-2A-terminal.md`, `design-notes-1F-activescripts.md`, `design-notes-2C-editor.md` (verbatim mock geometry/colors/states). Note: 2A mock's target panel shows Money+Security bars only (no RAM/ports bars).

**Exploration facts** (verified; implementers re-verify against source): Terminal output = flat `Terminal.outputHistory: (Output|Link|RawOutput)[]` capped by `Settings.MaxTerminalCapacity` (Terminal.ts:132,144); command echo printed by TerminalInput.tsx:248; history = `Terminal.commandHistory` (50 max) synced to `Player.terminalCommandHistory`; no Ctrl+R (↑ does prefix search w/ `Settings.EnableHistorySearch`); `Link` type = clickable-hostname precedent; `ls` uses `printRaw` React nodes; timed actions (hack/backdoor/grow/weaken/analyze) via `Terminal.timedAction` returning `{cancel, finished, getProgressText}` (no numeric progress exposed); NO earned-knowledge tracking exists anywhere; analyze reads live Server fields at completion. ActiveScripts: `workerScripts: Map<pid, WorkerScript>`, income/s = `onlineMoneyMade/onlineRunningTime`, groups by `hostname===Home` / `isHacknetServer` / `purchasedByPlayer` / `hasAdminRights`; logs via `LogBoxEvents.emit(runningScript)`; kill via `killWorkerScriptByPid` / `killAllScripts`; 400ms `useRerender`; existing tabs Active/RecentlyKilled/RecentErrors. Editor: module-level `openScripts: OpenScript[]`, dirty = `isUnsavedFile`, RAM via context `updateRAM`→`calculateRamUsage` (300ms debounce), NS IntelliSense from injected netscript.d.ts (no manual providers), `editorRef.current` exposes `IStandaloneCodeEditor`, NS API search = `DocumentationAutocomplete` in Toolbar, vim mode + OptionsModal exist and must keep working.

---

### Task 8: Terminal — command blocks + history panel + input restyle

**Files:** Modify `src/Terminal/OutputTypes.tsx` (+`CommandBlockStart` class: command, hostname, cwd, Date.now timestamp), `src/Terminal/ui/TerminalInput.tsx` (echo → append CommandBlockStart instead of plain print; restyled input per mock: bordered field, green mono prompt, hint row with REAL bindings only — `↹ complete · ↑↓ history · Ctrl+K palette`), `src/Terminal/ui/TerminalRoot.tsx` (render-time grouping + 2-col layout), `src/Terminal/Terminal.ts` (minimal: session-history hook + expose numeric progress on TerminalAction: store start/duration in timedAction), `src/Settings/Settings.ts` (+`PinnedTerminalCommands: string[]` default `[]`). Create `src/Terminal/ui/CommandHistoryPanel.tsx`, `src/Terminal/ui/CommandBlock.tsx`, `src/Terminal/sessionHistory.ts` (UI-layer module: `{command, timestamp}[]` pushed from executeCommands; not saved).

**Requirements:** Render-time grouping: iterate outputHistory; each `CommandBlockStart` opens a card (per 2A notes: radius 10, header `❯ command` mono + timestamp `textFaint` + collapse chevron; body = EXISTING item rendering unchanged — ANSIITypography/RawOutput/Link all verbatim); items before the first block render bare (banner). Collapse state per-block, runtime-only, default expanded; collapse animation 160ms. While `Terminal.action !== null` the newest block gets a RUNNING tag + progress bar (numeric fraction from the new start/duration getters; bar per mock cyan/green gradient — the ONE permitted gradient). Autoscroll behavior preserved (scrollHook pattern), including with cards. History panel (250px left, per notes): search filter; PINNED section (star toggle persists to `Settings.PinnedTerminalCommands`, `↻` re-runs via `Terminal.executeCommands` — disabled while `Terminal.action` active; gold star `accentGold`); THIS SESSION from sessionHistory (timestamps, current-input highlight per notes); EARLIER section from `Player.terminalCommandHistory` minus session items, dimmed, no timestamps. Click = re-run (disabled during action), Shift+click = paste into input (wire via a callback prop to TerminalInput's setValue). Footer hint per mock but honest ("Click to re-run · ⇧click to paste · ★ pin"). No Ctrl+R claims anywhere. MaxTerminalCapacity splice still works (grouping is render-time so no leak). Perf: block rendering must not regress the 25ms-debounced TerminalEvents path; React.memo CommandBlock on (item identity, collapsed).

**Tests:** grouping logic as pure function (`groupOutputHistory(items)` exported: banner-before-first-block, N blocks, async prints land in latest block); sessionHistory push/dedupe; pin/unpin persistence; CommandBlockStart construction. Render test: blocks render, chevron collapses, pinned re-run disabled during action.

---

### Task 9: Terminal — target panel + revelation snapshots + quick actions

**Files:** Create `src/Terminal/ui/TargetPanel.tsx`, `src/Terminal/serverSnapshots.ts`. Modify `src/Terminal/commands/analyze.ts` + `scananalyze.ts` (one line each at completion: `recordServerSnapshot(server)`), `src/Terminal/ui/TerminalRoot.tsx` (3rd column, 280px).

**Requirements:** `serverSnapshots.ts`: UI-layer module (runtime-only, NOT saved — document why: derived-state cache, dies on reload honestly) `Map<hostname, {moneyAvailable, moneyMax, hackDifficulty, minDifficulty, requiredHackingSkill, numOpenPortsRequired, openPortCount, maxRam, timestamp}>`; `recordServerSnapshot(...)` captures ONLY what the command actually printed, tagged with a `source` field: `analyze` (connected server) records the full snapshot incl. money/security; `scan-analyze` records only what it prints per server (root/skill/ports/RAM — NO money/security; those fields are optional in the snapshot type). The panel renders money/security bars only when present, and the stamp names the actual source command. [AMENDED after Task 9 review: the original text mandated full snapshots from scan-analyze, which leaks stats the command never printed — honesty constraint outranks.] TargetPanel (280px right, per 2A notes): "CONNECTED TO" + hostname mono cyan; ALWAYS-known facts only: admin rights, backdoor status (both visible in-game via prompt/commands), RAM used/max (freely shown by existing `free` command — cite in comment). Money bar (gold) + Security bar (red, current vs min) ONLY from snapshot, stamped `as of analyze · HH:MM — run again to refresh` (timestamp via existing Settings time format if one exists, else locale). No snapshot → UNDISCOVERED card per mock: states what isn't known and that `analyze` reveals it. Quick actions (hack/weaken/grow/backdoor buttons): submit the SAME terminal command via `Terminal.executeCommands`, disabled while `Terminal.action !== null` or no admin rights; they add zero capability. Panel updates on TerminalEvents + connect. Honesty rule: NOTHING from live Server fields except the always-known trio above; snapshot values render frozen (that's the point).

**Tests:** serverSnapshots record/get; TargetPanel honesty (no snapshot → no money/security values anywhere in DOM; snapshot → frozen values + stamp; live server money changes don't leak). Quick-action disabled states.

---

### Task 10: Active Scripts (1F)

**Files:** Rework `src/ui/ActiveScripts/` (ActiveScriptsPage, ServerAccordion→ServerGroup, ServerAccordionContent, WorkerScriptAccordion→ScriptRow, ScriptProduction→header card). Create `src/ui/ActiveScripts/NetworkRamBar.tsx`.

**Requirements (per 1F notes):** Keep ActiveScriptsRoot tabs + filter + pagination mechanics (mock omits them; they're load-bearing for 1000s of scripts — keep, restyled). Header card: Network RAM stacked bar 14px, 2px gaps, segments home `accentCyan`→purchased→rooted→hacknet→free (mock's 4 blues have no tokens: derive the 3 darker steps from accentCyan via documented alpha/mix or hardcode in a single data module with token-mapping comment — follow Stage 1 derivation precedent), used/total labels, total income/s (existing `Player.scriptProdSinceLastAug/playtimeSinceLastAug` rate) + total exp/s (sum `onlineExpGained/onlineRunningTime` over workerScripts). Server groups collapsible per notes: chevron, mono server name (cyan home per mock: expanded `accentCyan`/collapsed muted), inline RAM bar 180px + used/total, collapsed shows script count + group income. Script rows 7-col grid per notes: name mono · args muted (formatted like existing title logic) · threads · RAM (`formatRam`) · income/s gold · exp/s cyan · `logs` ghost + `kill` red-border ghost (existing LogBoxEvents/killWorkerScriptByPid). "Kill all scripts" red ghost top-right (existing killAllScripts + its confirmation if any). 400ms rerender kept. Group classification: Home/purchased/rooted/hacknet per exploration facts; unrooted-with-scripts (edge: scripts run only with admin — rooted covers it).

**Tests:** group classification pure fn (home/hacknet/purchased/rooted/free RAM aggregation vs GetAllServers fixtures); exp/s totals; NetworkRamBar segment math (zero-RAM guard, 2px gap phantom on zero segments — Stage 1 dashboard had this bug class). Render: rows show formatted values; kill/logs fire existing APIs.

---

### Task 11: Script Editor shell (2C part 1) — activity bar, explorer, tabs, status bar

**Files:** Rework `src/ScriptEditor/ui/ScriptEditorRoot.tsx` layout; create `src/ScriptEditor/ui/ActivityBar.tsx`, `ExplorerPanel.tsx`, `OutlinePanel.tsx`, `StatusBar2C.tsx` (rename/replace vim-only StatusBar carefully — vim status must keep working inside the new bar); restyle `Tabs.tsx` (dirty dots per mock, keep drag/drop + filter).

**Requirements (per 2C notes):** Activity bar 46px (34px buttons, inset 2px cyan active indicator): Explorer / Search (Task 12 wires; render disabled tooltip "coming in this stage" until then—NO, render only Explorer now, add Search in Task 12) — keep it minimal: Explorer toggle, Settings (existing OptionsModal), NS API docs (existing DocumentationAutocomplete relocated). Explorer 218px: files on `currentScript`'s server grouped by folder (server.scripts/textFiles), click opens (existing open-file machinery from Router context/`ScriptEditorRoot` — reuse its openScript path); OTHER SERVERS section collapsed: servers with file access (backdoored/purchased/admin — reuse `addReachableServerNames`-style filter, cite) + file counts, expand → their files open too; OUTLINE panel bottom (flex:1 spacer per notes): symbols of active file via TS worker `getNavigationTree`/monaco DocumentSymbol — if the worker API is awkward, regex top-level `function/const/class` fallback is acceptable v1 (document). Tab strip: dirty dot (existing `isUnsavedFile`), active tab per notes, keep react-beautiful-dnd. Status bar (bottom, per notes): problems count (monaco markers `onDidChangeMarkers`), live RAM cost moved from Toolbar (`ram` from ScriptEditorContext; gold, red when exceeds `GetServer(currentScript.hostname).maxRam` per mock "fits home (128GB)" phrasing), file/lang, Ln/Col (cursor listener), vim status segment, "Run ▸ server" button (existing run action if one exists in Toolbar — else save+snackbar deferred, document). Monaco editor untouched functionally: vim mode, autosave, themes, options modal all keep working. Toolbar's replaced elements removed without losing any existing capability (beautify button etc. relocate into status bar or activity bar).

**Tests:** explorer file-tree builder pure fn (folders grouped, other-servers filter honest); outline extraction fallback; status-bar RAM fits/exceeds logic. Render: tabs dirty dot, status bar segments.

---

### Task 12: Script Editor (2C part 2) — quick-open, full-text search, bottom panel

**Files:** Create `src/ScriptEditor/ui/QuickOpen.tsx`, `SearchPanel.tsx`, `BottomPanel.tsx` (tabs: Problems / NS API / Logs). Modify ActivityBar (Search icon), ScriptEditorRoot (Ctrl+P binding inside editor via `editor.addCommand`, panel mount).

**Requirements:** QuickOpen (Ctrl+P when editor focused; styled like CommandPalette — reuse its ranking helpers if exported, else share a module): all content files across accessible servers (same honest filter as explorer), server badge per result, Enter opens. NOT the shell Ctrl+K (pages) — keep both working (Ctrl+P only registered on the editor). Full-text search panel (Ctrl+Shift+F in editor): query over accessible servers' script/text contents, results grouped by file w/ line preview, click → open at line (`editor.revealLineInCenter` + setPosition). Case-sensitive toggle; cap results (e.g. 500 matches) with "capped" notice — no silent truncation. BottomPanel 130px per notes, tabs: Problems (marker list, click → goto), NS API (existing DocumentationAutocomplete + doc links relocated), Logs (list running scripts on current file's server via workerScripts filter; click → `LogBoxEvents.emit` — reuses floating log windows; embedding full tail = out of scope, document). Split editor + embedded terminal tab: OUT OF SCOPE this stage (document as deferred — handoff allows "wherever possible with Monaco built-ins"; splits need model-sharing work that risks vim/autosave regressions).

**Tests:** search fn (multi-server fixtures, cap, case toggle); quick-open ranking/filter honesty. Render: bottom panel tabs switch; problems click navigates.

---

### Task 13: Stage-2 verification + final review + push

Gates all green (tsc, full jest, lint, `npx webpack --mode development`); hex sweep over touched dirs; manual checklist for human (terminal blocks incl. async script prints, pin/re-run, target panel reveal flow, quick actions, active-scripts groups at scale, editor: vim/autosave/themes/options regression, quick-open, search, RAM/problems status); whole-branch review (Stage 2 diff) on most capable model; single fix wave; re-verify; push.

**Deliberate deviations to flag:** history panel "EARLIER" instead of mock's dated sessions (no historical timestamps exist); no Ctrl+R (never existed — hint rows honest); target-panel snapshots runtime-only; RAM/ports bars omitted from target panel (mock itself omits them); split editor + embedded terminal deferred; Logs tab opens floating windows; **analyze snapshot records/displays moneyMax + minDifficulty even though the command prints only current money/security** — sanctioned semantic: analyze is the revelation event for that server's stats, and the bars need their maxima (Task 9 review ruling).

**Manual checklist additions (final review):** Ctrl+K is a Monaco chord prefix by default — verify whether the shell palette opens while the editor has focus (predates Stage 2); Ctrl+Shift+F under vim mode; bash-hotkey Ctrl+K vs input hint; NS API popover→tab UX; Monaco relayout with bottom panel.
